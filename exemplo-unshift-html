<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Exemplo de unshift em arrays</h1>
    <p>Criando um array e adiconando item no inicio do array</p>
    <script>
        // Criando um array
        let listaPokemon = ['Pikachu', 'Charmander']

        // Adicionando o pokemon Squirtle no inicio do array
        listaPokemon.unshift('Squirtle')

        console.log(listaPokemon)

        console.log('=============================================')
        listaPokemon.unshift('Mewtwo', 'Eevee')

        console.log(listaPokemon)
    </script>
</body>
</html>